apiVersion: v1
kind: Service
metadata:
  name: ai-sre-demo
  namespace: ai-sre
  labels:
    app.kubernetes.io/name: ai-sre-demo
    app.kubernetes.io/managed-by: argocd
spec:
  type: ClusterIP
  selector:
    app: ai-sre-demo
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
